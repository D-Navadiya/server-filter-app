export const screenNames = Object.freeze({
  filterServer: 'filter-server-screen',
  filterSettings: 'filter-settings',
});

export const ScreenTitles = Object.freeze({
  filterServer: 'Filtered Server List',
  filterSettings: 'Choose Filter Settings',
});
